# testcase/ (placeholder)

Owned exclusively by the QA Tester agent. Populated by running
`agentic-builder run` (or `resume`/`dry-run`) against real requirements in
`requirements/` -- not pre-populated with example test cases. See the
repository root [README.md](../README.md) for how the orchestrator works.
