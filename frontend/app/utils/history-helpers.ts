// Intentionally empty. Re-exporting auto-imported symbols from utils/ causes
// duplicate auto-import registrations in Nuxt; pages import from the
// canonical modules (`~/utils/errors`, `~/types/api`) directly.
export {}
