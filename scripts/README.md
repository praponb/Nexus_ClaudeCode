# scripts/ (placeholder)

Owned by the Backend Developer agent (see [ASSUMPTIONS.md](../ASSUMPTIONS.md)
for the ownership rationale -- the spec's concurrency rules don't explicitly
assign this directory). Populated by running `agentic-builder run` (or
`resume`/`dry-run`) against real requirements in `requirements/`.
