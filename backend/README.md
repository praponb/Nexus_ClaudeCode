# backend/ (placeholder)

Owned exclusively by the Backend Developer agent (along with `scripts/`).
Populated by running `agentic-builder run` (or `resume`/`dry-run`) against
real requirements in `requirements/` -- not pre-populated with example
application code. See the repository root [README.md](../README.md) for how
the orchestrator works.
