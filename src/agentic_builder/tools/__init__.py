"""ADK tool functions used by the four agents.

Every tool in this package is a plain, type-hinted (async or sync) function
so ADK's automatic function-calling can build a schema for it directly --
none of them require manual ``FunctionTool`` wrapping.
"""
