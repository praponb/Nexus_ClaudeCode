# Input requirements (empty placeholder)

This directory is where real, user-authored requirement Markdown files go:

- `specification.md` (required)
- `layout.md` (required)
- `front-back-end-stack.md` (required)
- any additional `*.md` files (optional, also considered by the Team Lead)

It starts empty in this repository -- no real project requirements exist
yet. The input directory is configurable via `--input-dir`; this is only the
default location.

For example/test-only requirement sets used by the automated test suite,
see `tests/fixtures/` instead -- those are never real input and are never
read by the default CLI invocation.
